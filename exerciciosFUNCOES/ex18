function transformarStatus(status) {
    const novoArray = [];

    for (const item of status) {
        novoArray.push(item ? "Concluído" : "Pendente");
    }

    return novoArray;
}

console.log(transformarStatus([true, false, true, false]));